import React, { PropTypes } from 'react';

const styles = {
  root: { padding: 24 }
}

function NoResult(props) {
  
  return (
    <div
      style={styles.root}
    >
      No Result
    </div>
  );
}

export default NoResult;
